package ifsp.aula4;

public class DatasArmazenadas {
    public static void main(String[] args) {
        // scanner sc =  new Scanner(System.in);
        // System.out.println("Digite uma frase: ");
        // String frase = sc.nextLine();

        // StringTokenizer t = new StringTokenizer(frase,"");

        // while( t.hashMoreElements() ) {
        //     String temp = t.nextToken();
        //     if(mapa.containsKey(temp)){
        //         Integer valor = mapa.get(temp);
        //         valor = valor + 1;
        //         mapa.put(temp, valor);
        //     }
        //     else
        //         mapa.put(temp, 1);
        // }

        // Set<String> palavras = mapa.keySet();

        // for( String w; palavras)
        //     System.out.println(w + "." + mapa.get(w));

        // Scanner.close();
    }

}